# radarScan
mmWave 60GHz radar Scan 

✅ 1. Install libgpiod (library + tools)
sudo apt update
sudo apt install gpiod libgpiod-dev

gpiodetect
gpioinfo

sudo usermod -aG gpio $USER
newgrp gpio

groups

